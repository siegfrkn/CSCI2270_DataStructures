Write a function, helloDataStructures(...), that takes a string as parameter, name, and returns another sentence that is the concatenation of "Hello" and name.

Make sure to check that the sentence being fed into the function is not empty. If the sentence is empty the function should return "Invalid sentence"



Example

string name = "Giorgione";
cout<<helloDataStructures(name)<<endl;
Output

Hello Giorgione









#include <iostream>
#include <string>
using namespace std;

string hello, concat, sentence;
string name = "Giorgione";

string helloDataStructures(string name)
{
    //cout<<"What's your name? ";
        
    if (name.empty())
    {
        cout<<"Invalid sentence";
        string hello = "";
        string name = "";
        concat = hello + name;
    }
    else
    {
        string hello = "Hello ";
        concat = hello + name;
    }
    
    return concat;
}


